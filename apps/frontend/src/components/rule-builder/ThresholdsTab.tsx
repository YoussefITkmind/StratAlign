import { RuleBuilderPanel } from "@/components/rule-builder/RuleBuilderPanel";

/** Compatibility entry point for the legacy KPI detail shell retained for Prompt 2.9. */
export default function ThresholdsTab() {
  return <RuleBuilderPanel />;
}
